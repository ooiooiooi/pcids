# PCIDS 局域网烧录器节点扫描配置
# 修改后下一次点击“扫描所有节点”即生效，无需重新打包或重启程序。
#
# discovery_cidrs 为空时：自动扫描所有“已启用的物理网卡”所在网段，
# 并自动排除 VMware、VPN、Docker、WSL 等虚拟网卡。
# 如需固定扫描范围，填写一个或多个 CIDR，例如：
# discovery_cidrs:
#   - 192.168.1.0/24
#   - 192.168.2.0/24
discovery_cidrs: []

# 下位机 PCIDS Agent 的监听端口。默认安装保持 8000。
port: 8000
